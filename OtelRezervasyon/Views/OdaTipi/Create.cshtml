@model OtelRezervasyon.Models.ODATIPI

@{
    ViewBag.Title = "Create";
    Layout = "~/Views/Shared/_LayoutAdmin.cshtml";
}

<h2>Yeni Oluştur</h2>

@using (Html.BeginForm()) {
    @Html.AntiForgeryToken()
    @Html.ValidationSummary(true)

    <fieldset>
        <legend>ODATIPI</legend>

        <div class="editor-label">
            @Html.LabelFor(model => model.ADI)
        </div>
        <div class="editor-field">
            @Html.EditorFor(model => model.ADI)
            @Html.ValidationMessageFor(model => model.ADI)
        </div>

        <div class="editor-label">
            @Html.LabelFor(model => model.RESIMI)
        </div>
        <div class="editor-field">
            @Html.EditorFor(model => model.RESIMI)
            @Html.ValidationMessageFor(model => model.RESIMI)
        </div>

        <div class="editor-label">
            @Html.LabelFor(model => model.FIYAT)
        </div>
        <div class="editor-field">
            @Html.EditorFor(model => model.FIYAT)
            @Html.ValidationMessageFor(model => model.FIYAT)
        </div>

        <p>
            <input type="submit" value="Create" />
        </p>
    </fieldset>
}

<div>
    @Html.ActionLink("Back to List", "Index")
</div>

@section Scripts {
    @Scripts.Render("~/bundles/jqueryval")
}
